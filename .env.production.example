# Environment Variables Configuration for DigitalOcean App Platform
# 
# Set these as secrets in your DigitalOcean App Platform dashboard:
# 1. Go to your app in the DO dashboard
# 2. Navigate to Settings > App-Level Environment Variables
# 3. Add these as encrypted variables

# Required Gradient AI API Configuration
GRADIENT_API_KEY=your_gradient_api_key_here
GRADIENT_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Optional: If you want to use OpenAI as fallback (not currently used)
OPENAI_API_KEY=your_openai_api_key_here

# Production Settings (automatically set in app.yaml)
DEBUG=False
FRONTEND_ORIGIN=https://your-app-domain.ondigitalocean.app
DATA_DIR=/tmp/data
VECTOR_DIR=/tmp/data/vector_store
USE_GRADIENT=True
GRADIENT_API_BASE=https://inference.do-ai.run
GRADIENT_MODEL=gpt-4o-mini
USE_GRADIENT_EMBEDDINGS=True

# Frontend Environment Variables
VITE_API_URL=https://your-backend-url.ondigitalocean.app